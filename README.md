# lamoon-p.github.io

Front page for <https://lamoon-p.github.io> — an index of the browser-only tools.

One static file, no dependencies, no CDN, no fonts, no analytics. Each tool lives
in its own repo and is served as a GitHub Pages project page under this domain.

Deployed from `main` / `/` (Pages `build_type: legacy`).
